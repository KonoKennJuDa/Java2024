    // Iterable - самый первый интерфейс, самый верхний, после идет Collection
    // Collection - делиться еще на List, Set и Queue
    // Queue - очередь. Последний пришел, первый ушел. Last in, first out
    // List - список, упорядоченый (order), индексированный (by index). ArrayList, LinkedList
    // Linkedlist - это связанный/двусвязанный(каждый элемент знает о следующем и о предыдушем элеменете) список. список начинается с head-v-v-v-v-tail
    // ArrayList - динамический массив. Умеет изменять свой размер в зависимости от требований
    // Set - множество уникальных значений (unique), не упорядоченный, не можем получить доступ по индексу
    // Map - отдельный интерфейс, equals, hashCode. Позволяет хранить пары ключ, значение. Ключ уникальный, значение не обязательно уникальное
    // TreeMap и TreeSet - реализации дерева, чтобы был предсказуемый хеш